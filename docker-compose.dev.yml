services:
  app:
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload --reload-dir /app/backend --reload-dir /app
    volumes:
      - ./:/app
      - /var/run/docker.sock:/var/run/docker.sock
